export function OrdersWorkspace({ role }: { role: string }) {
  const stages = ["Pending confirmation", "In production", "Ready to ship", "Delivered"];
  return <main className="min-h-screen bg-[#f5f3ee] text-[#203027]"><header className="border-b border-[#d9d8cf] bg-white"><div className="mx-auto flex max-w-[1440px] items-center justify-between px-5 py-5 sm:px-8"><div><p className="text-[10px] font-bold tracking-[0.18em] text-[#a8472c] uppercase">Odhvica operations</p><h1 className="mt-1 font-[family-name:var(--font-cormorant)] text-3xl tracking-[-0.04em]">Order workspace</h1></div><p className="text-xs font-semibold capitalize text-[#667069]">{role} staff session</p></div></header><section className="mx-auto max-w-[1440px] px-5 py-8 sm:px-8"><p className="max-w-2xl text-sm leading-6 text-[#667069]">Every order will preserve independent payment, fulfilment and post-purchase states. Staff cannot mark a browser-success screen as paid; provider confirmation is required before fulfilment.</p><div className="mt-8 grid gap-4 md:grid-cols-4">{stages.map((stage) => <div key={stage} className="rounded-2xl border border-[#d9d8cf] bg-white p-5"><p className="text-[10px] font-bold tracking-[0.15em] text-[#a8472c] uppercase">{stage}</p><p className="mt-6 font-[family-name:var(--font-cormorant)] text-5xl">0</p><p className="mt-2 text-xs text-[#667069]">Orders appear after configured checkout activation.</p></div>)}</div><div className="mt-7 rounded-2xl border border-dashed border-[#bdbdb4] bg-white/70 p-8"><h2 className="font-[family-name:var(--font-cormorant)] text-4xl tracking-[-0.04em]">No persisted orders yet.</h2><p className="mt-3 max-w-2xl text-sm leading-6 text-[#667069]">The protected order lifecycle, payment snapshots, fulfilment events, returns and refunds are ready in the Phase 4 schema. This workspace will populate from the production PostgreSQL checkout flow after provider accounts and store configuration are enabled.</p></div></section></main>;
}
